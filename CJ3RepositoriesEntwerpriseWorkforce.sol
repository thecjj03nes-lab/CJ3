pragma solidity ^0.8;

// import InstilledInteroperability;
// import buyPi function;
// import KYC/AML so too many accounts arent nested



// ADP, WORKFORCE, LinkedIn, ETC

contract EmployeePiPay ={


// create company account, kyc/aml, bbb

function createCompanyAccount ={
company name = $companyName;
first name = $firstName;
last name = $lastName;
headquarters address = $hqAddress; 
return companyAccount;}

// create worker account
function createWorkerAccount ={
first name = $firstName;
last name = $lastName;
address = $address;
bank details = $bankingDetails;
pi wallet address = $piAddress;
return workerAccount;

}

// choose worker base pay
const workerBasePay = minimumWage || salary || custom ;
return worker[basePay];


// choose worker timeIn/ timeOut
const workerTimeLog ={ 
console.log(time-in);
console.log(time-out);
time-out - time-in = hoursWorkedInADay;
if time-in = 8am,
then time-out = 6pm,
return 6pm - 8am = 10 hours;
hoursWorkedInADay = 10 hours;
return hoursWorkedInADay;
}

// connect worker wallet[address]
connectWallet || copy & paste manually;
return worker[wallet.address];



// pay worker pi [USD denomination]
     (require == workerTimeLog);
if payPeriod = every 2 weeks,
then the sum of hoursWorkedInADay,
return hoursWorkedInAPayPeriod;
hoursWorkedInAPayPeriod * worker[basePay] = worker[paycheckBalanceInUSD];
if hoursWorkedInAPayPeriod = 60 hours,
&& if worker[basePay] = $15,
then return 60 hoursWorkedInAPayPeriod * $15 worker[basePay] = $900 InUSD;
return worker[paycheckBalanceInUSD];

await worker[paycheckBalanceInUSD] then return function buyPi;
await function buyPi then transfer paycheckBalanceInPi to worker[wallet.address];
return worker[paycheckBalanceInPi];


}
