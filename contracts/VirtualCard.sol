mapping(address => card) virtualCard;
mapping(card => vendor) payWithCard;
mapping(sender => card) acceptWithCard;

$USDC, native gas token, or platform native token fallback,

keccak(0-256), random(random number + random number + random number + random number ÷ random number = random number(-/+),



msg.sender addressCard:{
card number:
expirationDate:
pin:
}

