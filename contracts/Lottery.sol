a lottery opens for a set amount if time,



people deposit money & it gets held as yUSDC, yBTC and etc Assets in the marketcap;


the timer ends: async randomLotteryPick(random + random + random + random + random ÷ random + random + random + random + random + random + random + random ÷ random × random + random ÷ random = random;

submit results;
only LotteryPick with true results, timestamps, and allocation as bool verification (true/false) to async send them their funds;

setLotteries on timer:
allocatePotInterest: 20% $GETVL,
