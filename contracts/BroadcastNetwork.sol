import instilledinteroperability;
import world-wide-web;
import google;
import os;
import zeropointwifi;


network name:,
url: (choose a domain that you own or doesn't exist, no.coms; something.likethis),
hotspot url name:,

generate broadcast station;
map(broadcast station => url);

generate hotspot;
map(hotspot => hotspot url + url)
